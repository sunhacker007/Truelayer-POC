# TrueLayer BNPL POC 思路说明

**版本:** v1.0 | **日期:** 2025-05 | **作者:** POC Team

---

## 一、背景与目标

BNPL（先买后付）业务的核心风控依赖两件事：

1. **还款日监控**：在借款人还款日（T+3）自动检查其银行账户是否有出账记录，判断是否已还款
2. **信用评估（IBS）**：在放款前通过银行流水分析借款人的收支行为，辅助授信决策

TrueLayer 是一家 Open Banking 数据服务商，可通过 API 获取用户银行账户的交易数据。本 POC 的目标是验证：**TrueLayer 能否支撑上述两个核心场景，以及 UK 和 DE 两个市场的可行性差异。**

---

## 二、整体思路

POC 分三层递进验证，从"能不能连上"到"数据够不够用"：

```
Layer 1  连通性验证     ── 先跑通 API，确认基础可用
    ↓
Layer 2  机制验证       ── 验证 D+3 监控的核心技术路径
    ↓
Layer 3  数据质量验证   ── 验证能否提取 IBS 评分所需特征
```

---

## 三、三层验证详解

### Layer 1：连通性验证

**问题：** TrueLayer 的 OAuth2 授权流程和四个核心接口能否跑通？

**做法：**
- 用 TrueLayer Sandbox 的 Mock Bank（`uk-cs-mock`，账户 john/doe）完成 OAuth2 授权码流程，获取 access_token 和 refresh_token
- 依次调用四个接口：账户列表、交易记录、余额、身份信息
- 验证 Token 能否在后台静默刷新（不需要用户重新登录）

**为什么重要：** Token 刷新是 D+3 监控的前提——系统需要在用户不在线的情况下自动拉取数据。

---

### Layer 2：D+3 还款监控机制验证

**问题：** 在还款日（T+3），系统能否在用户无感知的情况下，后台自动拉取其出账交易？

**UK 场景（C3）：**

UK 在 FCA 新规下支持长期 Token（`john/eternal`），模拟连续三期还款日的静默拉取：
- 每次拉取前先刷新 Token
- 全程不需要用户重新授权
- 验证三期均可成功获取数据

**DE 场景（C4）：**

DE 银行受 PSD2 强制要求，每 90 天必须重新做 SCA（强客户认证）。Token 到期后 refresh 会返回 `invalid_grant` 错误。验证：
- 错误码是否清晰、可程序化处理
- 重新授权流程是否可用

**还款识别逻辑：**

从出账交易中识别"归还给 Klarna 的款项"，采用双策略：
1. 对手方账号精确匹配（sort_code + account_number）→ 最可靠
2. 交易描述关键词匹配（KLARNA / REPAYMENT 等）→ 降级方案

---

### Layer 3：数据结构与特征提取验证

**问题：** TrueLayer 返回的数据字段是否完整，能否计算 IBS 评分所需的六维特征（RFMQTD）？

**D1 字段完整率审计：**

对 1788 笔交易逐字段统计非空率，与业务最低要求对比：

| 字段 | 业务用途 | 要求 |
|------|---------|------|
| amount / timestamp | 基础收支判断 | 100% |
| transaction_classification | IBS 分类特征 | ≥70% |
| merchant_name | 还款识别辅助 | ≥60% |
| running_balance | 余额趋势计算 | ≥80% |

**D2 分类体系验证：**

检查关键风险类别是否存在（Gambling 赌博、Housing 住房等），这些是 BNPL 风控的核心信号。

**D3 RFMQTD 六维特征：**

| 维度 | 含义 | 计算方式 |
|------|------|---------|
| R 近度 | 最近一笔收入距今天数 | 收入类交易最近时间戳 |
| F 频度 | 月均交易笔数 | 总笔数 ÷ 月数 |
| M 金额 | 月均净现金流 | 月均（收入 - 支出） |
| Q 质量 | 收入稳定性 | 月收入变异系数 CV |
| T 趋势 | 余额变化趋势 | 余额序列线性回归斜率 |
| D 多样性 | 消费品类丰富度 | 活跃分类数 |

---

## 四、UK vs DE 差异对比

| 能力 | UK | DE |
|------|----|----|
| OAuth2 连通性 | ✅ | ✅ |
| Token 长期有效 | ✅ FCA 新规支持 | ❌ 90 天 SCA 强制重认证 |
| D+3 静默拉取 | ✅ 完全可行 | ⚠️ 需产品设计提前通知用户 |
| transaction_classification | ✅ 支持 | ❌ 官方确认不支持 |
| merchant_name enrichment | ✅ 部分银行支持 | ❌ 不支持 |
| 交易历史深度 | 可拉取历史数据 | 部分银行仅 90 天 |

---

## 五、Sandbox 的边界与补偿

TrueLayer Sandbox 只提供一个 Mock Bank（`uk-cs-mock`），不是真实银行。因此：

| 局限 | 影响 | 补偿方案 |
|------|------|---------|
| Mock Bank 不返回 merchant_name | D1 该字段显示 0% | 真实 UK 银行账户验证 |
| Mock Bank 不做分类标注 | D2 分类为空 | 真实账户验证；DE 自建关键词规则 |
| 无法模拟 SCA 自然到期 | C4 无法完整验证 | 申请 DE 真实测试账户 |
| 只有一家 Mock Bank | 无法测跨行差异 | 向 TrueLayer 商务申请多银行接入 |

---

## 六、POC 结论

**UK 市场：推荐推进**
- D+3 监控机制技术完全可行
- RFMQTD 评分卡 5/6 维度可直接计算（第6维依赖分类，待真实数据验证）
- 集成复杂度低，2 周可完成生产级集成

**DE 市场：可行，需额外工程投入**
- 核心 API 连通无障碍
- SCA 重认证是产品层面问题，需在 D+87 提前触达用户（短信/邮件提醒）
- 需自建分类规则（1-2 周工程量）
- 需向 TrueLayer 申请真实 DE 银行测试账户做最终确认

---

## 七、下一步行动

- [ ] 向 TrueLayer 商务申请 DE Sparkasse / Deutsche Bank 测试账户
- [ ] 用真实 UK 银行账户验证 merchant_name 和 classification 实际覆盖率
- [ ] 设计 DE 市场 SCA 到期提醒流程（D+87 节点）
- [ ] 与 Klarna BD 确认收款账号（sort_code + account_number），完善还款识别逻辑
- [ ] 要求 TrueLayer 提供匿名真实交易数据样本，验证 IBS 分类准确率
